<%= form_for(task_priority) do |f| %>
  <% if task_priority.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(task_priority.errors.count, "error") %> prohibited this task_priority from being saved:</h2>

      <ul>
      <% task_priority.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :level %>
    <%= f.text_field :level %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
